import { TipoFoto2Pipe } from './tipo-foto2.pipe';

describe('TipoFoto2Pipe', () => {
  it('create an instance', () => {
    const pipe = new TipoFoto2Pipe();
    expect(pipe).toBeTruthy();
  });
});
